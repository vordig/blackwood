import { OutlinedButtonDirective } from './outlined-button.directive';

describe('OutlinedButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new OutlinedButtonDirective();
    expect(directive).toBeTruthy();
  });
});
